/*using Monster_University_GR2.Models;

public interface IUsuarioRepository
{
    List<User> GetAll();
    User GetByCedula(string cedula);
    string Create(User user);              // Antes bool, ahora string
    string Update(string cedula, User user); // Antes bool, ahora string
    string Delete(string cedula);          // Antes bool, ahora string
}*/