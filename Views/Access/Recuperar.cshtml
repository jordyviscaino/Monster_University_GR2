@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = null;
    ViewBag.Title = "Recuperar Contraseña";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title - Monster University</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link rel="stylesheet" href="~/css/Login.css" asp-append-version="true" />

</head>
<body>

    <div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="login-card" style="padding-top: 20px;">

            <div class="text-center mb-4">
                <h3 class="text-white">Recuperar Acceso</h3>
                <p class="text-light small">Ingresa tu correo institucional para restablecer tu clave.</p>
            </div>

            <form asp-controller="Access" asp-action="Recuperar" method="post">

                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger text-center p-2 small">@ViewBag.Error</div>
                }
                @if (ViewBag.Exito != null)
                {
                    <div class="alert alert-success text-center p-2 small">@ViewBag.Exito</div>
                }

                <label class="text-light fw-bold">Correo Electrónico</label>
                <div class="input-group flex-nowrap mb-4">
                    <span class="input-group-text bg-transparent text-white border-end-0">
                        <i class="fas fa-envelope"></i>
                    </span>
                    <input type="email" name="correo" class="form-control border-start-0"
                           placeholder="ejemplo@monster.edu.ec" required>
                </div>

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-warning text-dark fw-bold">ENVIAR CORREO</button>
                </div>

                <div class="text-center mt-4">
                    <a asp-controller="Access" asp-action="Login" class="text-decoration-none text-light small">
                        <i class="fas fa-arrow-left"></i> Volver al Login
                    </a>
                </div>

            </form>
        </div>
    </div>

</body>
</html>